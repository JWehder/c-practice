#include <stdio.h>
#include <stdlib.h>

int main() 
{
    double floaty = 2.3;
    char characterName[] = "Jake";
    int characterAge = 26;
    printf("There was once a guy named %s", characterName);
    printf("He is the age of %d", characterAge);
    printf("He really likes his name, %s", characterName);
    printf("he does not like being %d", characterAge);

    return floaty;
}